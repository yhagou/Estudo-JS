//? Array.every()

const numeros = [40, 34, 67, 89, 23, 10];

const todoAlgo = numeros.every((elemento) => (elemento) => 10);
console.log(todoAlgo);
console.clear();

//? Array.some()

const numeros2 = [-1, 3, 7, -3, 5];

const retorno = numeros2.some((elemento) => elemento > 0);
console.log(retorno);
